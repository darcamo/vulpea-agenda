# Vulpea Agenda

The `vulpea-agenda-wrapper` command enhances `org-agenda` by dynamically including org-mode files from the Vulpea database in `org-agenda-files`. Files are added if they contain any tags specified in `vulpea-agenda-tags`. Simply tag the relevant note, then use `vulpea-agenda-wrapper` in place of `org-agenda`.
